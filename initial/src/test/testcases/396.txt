int main() {
                int num;
                printf("\nNhap so dia:");
                scanf("%d", num);

                TOH(num - 1, "A", "B", "C");
                return (0);
            }

                    